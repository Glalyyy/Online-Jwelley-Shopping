@model List<Jewelly.Models.JewelTypeMst>
@{
    ViewBag.Title = "Jewel";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutPageAdmin.cshtml";
}

<div>
    <div class="container">
        <h1 class="_goldh1">Welcom to JewelTypeMst</h1>
       
    </div>
    <div>
        @if (ViewBag.SuccessMg != null)
        {
            <div id="msgAlert" style="margin-left: 3em;" class="alert alert-success">
                @ViewBag.SuccessMg
            </div>
        }
        else if (ViewBag.ErrorMg != null)
        {
            <div id="msgsAlert" style="margin-left: 3em;" class="alert alert-danger">
                @ViewBag.ErrorMg
            </div>
        }
    </div>
    <div>
        <table>
            <tr class="d-flex flex-row align-items-center flex-wrap" action="/Admin/JewelTypeMst/Jewel">
                <td colspan="2">
                    <a href="/Admin/JewelTypeMst/Create" class="mx-3 btn btn-warning text-center">+ Add Jewellry</a>
                </td>
                <td>
                    <input type="search"
                           class="form-control mx-6"
                           id="search"
                           name="search"
                           placeholder="search" />
                </td>
                <td>
                    <button type="submit" class="btn btn-primary mx-2">Go</button>
                </td>
            </tr>
            <br />
        </table>
        <table class="table _gold"><br />
            <thead>
                <tr>
                    <td>ID</td>
                    <td>Jewellery_Type</td>
                    <td>Edit</td>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.ID </td>
                        <td>@item.Jewellery_Type</td>
                        <td colspan="1">
                            <a href="/Admin/JewelTypeMst/Edit/@item.ID" class="btn btn-success text-center">Edit</a>
                        </td>

                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
<script>
    setTimeout(function () {
        $('#msgAlert').fadeOut('slow');
        $('#msgsAlert').fadeOut('slow');
    }, 2000);
    console.log()</script>

